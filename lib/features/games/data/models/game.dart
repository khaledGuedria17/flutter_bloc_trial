class Game {
  //att
  final String id, name, type, image, release;
  final int rating;
  //.constructor
  const Game(
    this.id,
    this.name,
    this.type,
    this.image,
    this.release,
    this.rating,
  );
}
